<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Payment Successful</title>
  <style>
    body{font-family: system-ui, -apple-system, "Segoe UI", Roboto, Arial; margin:0; background:#fffaf6}
    .wrap{max-width:800px;margin:40px auto;padding:28px;background:#fff;border-radius:12px;box-shadow:0 12px 40px rgba(16,24,40,0.06)}
    h1{margin:0;color:#0b7bff}
    .small{color:#555;margin-top:8px}
    .gift-box{margin-top:22px;padding:18px;border-radius:12px;background:linear-gradient(90deg,#fff8f0,#fff0fb);border:1px solid #ffe7e7}
    .link-row{display:flex;gap:10px;align-items:center;margin-top:12px}
    .link-input{flex:1;padding:10px;border-radius:8px;border:1px dashed #ddd;background:#fff}
    .btn{padding:10px 14px;border-radius:8px;background:#0b7bff;color:#fff;border:none;cursor:pointer}
    .btn-outline{background:transparent;border:1px solid #0b7bff;color:#0b7bff}
  </style>
</head>
<body>
  <div class="wrap">
    <h1>✅ Payment Successful</h1>
    <p class="small">Thanks, <strong><%= email %></strong> — your plan <strong><%= plan %></strong> is active now.</p>

    <% if (giftToken) { 
         const link = ('localhost:3000'|| '') + '/gift/' + giftToken;
    %>
      <div class="gift-box">
        <div style="display:flex;align-items:center;gap:12px">
          <div style="font-size:28px">💖</div>
          <div>
            <div style="font-weight:700">You bought a gift for <%= partnerEmail %></div>
            <div class="small">Share the link below with your partner — their subscription is already active. This link is just for them to see the surprise page.</div>
          </div>
        </div>

        <div class="link-row" style="margin-top:12px">
          <input id="giftLink" class="link-input" readonly value="<%= link %>"/>
          <button class="btn" onclick="copyLink()">Copy</button>
         <a class="btn btn-outline" 
   href="https://mail.google.com/mail/?view=cm&fs=1&to=<%= partnerEmail %>&su=You've received a Lovechat gift!&body=Hey! I've bought you Lovechat premium. Claim it here: <%= link %>" 
   target="_blank">
   Send via Gmail
</a>
<a class="btn btn-outline" 
   href="https://wa.me/?text=Hey! I've bought you Lovechat premium 💙 Claim it here: <%= link %>" 
   target="_blank">
   Share on WhatsApp
</a>
  </div>

        <div style="margin-top:10px" class="small">Link expires in 30 days. You can resend or revoke from your account page later.</div>
      </div>
    <% } %>

    <div style="margin-top:20px">
      <a class="btn" href="/">Go to Dashboard</a>
    </div>
  </div>

<script>
  function copyLink() {
    const el = document.getElementById('giftLink');
    navigator.clipboard.writeText(el.value).then(() => {
      alert('Gift link copied to clipboard — share it with your partner ❤️');
    }).catch(()=> alert('Could not copy, select the link and copy manually.'));
  }
</script>
</body>
</html>
